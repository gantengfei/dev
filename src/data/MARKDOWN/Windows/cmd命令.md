`del`删除文件夹中所以子目录中指定后缀文件
``` bash
# cmd 删除文件夹中所以子目录中指定后缀文件
del *.js.map /s
```

`cls`清除cmd窗口现有文本
``` bash
cls
```

`ctrl+c`中断任何正在运行的命令

`code ./`代码编辑器打开当前文件
``` bash
code ./
```

`rm`删除文件夹
``` bash
rm .\dist\
```
