# ➤ 判断元素是否隐藏
``` JavaScript
// 判断 CSS 属性 display:[none|block], 忽略 visibility:[true|false]
// 判断元素是否显示 true(显示) | false(隐藏)
$(element).is(":visible");

// 判断元素是否隐藏 true(隐藏) | false(显示)
$(element).is(":hidden");
```
