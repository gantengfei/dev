`hasOwnProperty(propertyName)` 方法
```
hasOwnProperty(propertyName)方法
是用来检测属性是否为对象的自有属性，如果是，返回true，否者false; 参数propertyName指要检测的属性名；
用法：object.hasOwnProperty(propertyName) // true/false

hasOwnProperty() 方法是 Object 的原型方法（也称实例方法）
它定义在 Object.prototype 对象之上，所有 Object 的实例对象都会继承 hasOwnProperty() 方法。
```

时间格式化-正则转换
``` JavaScript
// 2020-02-12 08:30:00
let pattern = /(\d{4})-(\d{2})-(\d{2}) (\d{2}):(\d{2}):(\d{2})/g;
format = '2020-02-12 08:30:00'.replace(pattern, '$1年$2月$3日$4时$5分$6秒');
// 20200212083000
let pattern = /(\d{4})(\d{2})(\d{2})(\d{2})(\d{2})(\d{2})/g;
format = '20200212083000'.replace(pattern, '$1-$2-$3 $4:$5:$6');
```

密码验证
``` JavaScript
let strongRegex = new RegExp("^(?=.*[0-9])(?=.*[a-zA-Z!@_#$%^&*()\-+=,.?]).{6,32}$", "g");
strongRegex.test(password)
// 密码必须包含数字，且必须包含字母或其它符号（!@_#$%^&*()-+=,.?）
// 密码的长度必须是 6-32 位
// 密码不少于6位
```

`splice()` 方法
``` JavaScript
let array = ['a','b','c'];
// 指定元素删除
array.splice(array.indexOf('b'), 1); // ['a','c']
// 指定元素位置插入
array.splice(array.indexOf('b'), 0, 'd'); // ['a','b','d','c']

webLangs = ['HTML', 'css', 'javascript', 'jQuery']
// 5.splice(start)：删除元素/插入元素/替换元素
// 删除元素：第二个参数传入你要删除几个元素 (如果没有传，就删除后面的所有的元素)
this.webLangs.splice(1,1) //第一个位置删除一位
// 替换元素：第二个参数，表示我们要替换几个元素，后面用于替换前面的元素
this.webLangs.splice(2, 2, 'js', 'jq')
// 插入元素：第二个参数，传入0，并且后面跟上要插入的元素
this.webLangs.splice(2, 0, 'sass', 'less')
```

`filter`/`map`/`reduce` 高阶函数
``` JavaScript
// 编程范式：命令式编程/声明式编程
// 编程范式：面向对象编程（第一公民：对象）/函数式编程（第一公民：函数）


// 函数式编程
const nums = [10, 20, 222, 40, 50, 235]
// ----- 1 -----
let total = nums.filter(v => v < 100).map(v => v * 2).reduce((pre, v) => pre + v, 0)
console.log(total);

// ----- 2 -----
// filter/map/reduce 高阶函数
// filter中的回调函数有一个要求：必须返回一个boolean值
// true:当返回true时，函数内部会自动将这次回调的value加入到新的数组中
// false:当返回false时，函数内部会过滤掉这次的value
let newNums = nums.filter((v, i) => v < 100)
console.log(newNums);
// map函数的使用
let new2Nums = newNums.map((v, i) => v * 2)
console.log(new2Nums);
// reduce函数的使用
// reduce作用对数组中所有的内容进行汇总
let total = new2Nums.reduce((preValue, value) => preValue + value, 0)
console.log(total);

// ----- 3 -----
//1.需求:取出所有小于100的数字
let newNums = []
for (let n of nums) {
  if (n < 100) {
    newNums.push(n)
  }
}
// 2.需求:将所有小于100的数字进行转化:全部*2
let new2Nums = []
for (let n of newNums) {
  new2Nums.push(n * 2)
}
// 3.需求:将所有new2Nums数字相加,得到最终的记过
let total = 0
for (let n of new2Nums) {
  total += n
}
console.log(total);
```
